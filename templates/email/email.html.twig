<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bienvenue{% endblock %}</title>
        
        
    </head>
    <body>
        <h1>Demande de devis n° {{ contact.id }} {{ contact.nom }} {{ contact.prenom }}</h1>
        <p>Email : {{contact.email}}</p>
        <p>Message : {{contact.message}}</p>
        
    </body>
    
</html>
